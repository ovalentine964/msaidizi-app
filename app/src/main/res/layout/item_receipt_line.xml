<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="8dp">

    <!-- Edited indicator (small dot) -->
    <View
        android:id="@+id/edited_indicator"
        android:layout_width="8dp"
        android:layout_height="8dp"
        android:background="@drawable/bg_edited_dot"
        android:visibility="gone"
        tools:visibility="visible"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_marginStart="4dp" />

    <!-- Item name — editable -->
    <EditText
        android:id="@+id/item_name_edit"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp"
        android:background="@null"
        android:textSize="16sp"
        android:textColor="@color/on_surface"
        android:hint="Jina la bidhaa"
        android:inputType="text"
        android:singleLine="true"
        android:contentDescription="Jina la bidhaa (Item name)"
        tools:text="Nyanya"
        app:layout_constraintStart_toEndOf="@id/edited_indicator"
        app:layout_constraintEnd_toStartOf="@id/item_price_edit"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- Price — editable -->
    <EditText
        android:id="@+id/item_price_edit"
        android:layout_width="80dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp"
        android:background="@null"
        android:textSize="16sp"
        android:textColor="@color/primary"
        android:textStyle="bold"
        android:gravity="end"
        android:inputType="numberDecimal"
        android:singleLine="true"
        android:contentDescription="Bei (Price)"
        tools:text="200"
        app:layout_constraintEnd_toStartOf="@id/remove_button"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- Remove button -->
    <ImageButton
        android:id="@+id/remove_button"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:layout_marginStart="8dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:src="@android:drawable/ic_menu_close_clear_cancel"
        android:padding="6dp"
        android:scaleType="centerInside"
        android:contentDescription="Ondoa bidhaa (Remove item)"
        app:tint="@color/error"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent" />

    <!-- Quantity label (if > 1) -->
    <TextView
        android:id="@+id/qty_label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="12sp"
        android:textColor="@color/on_surface_variant"
        android:visibility="gone"
        tools:text="x2"
        tools:visibility="visible"
        app:layout_constraintStart_toStartOf="@id/item_name_edit"
        app:layout_constraintTop_toBottomOf="@id/item_name_edit" />

    <!-- Divider -->
    <View
        android:layout_width="0dp"
        android:layout_height="1dp"
        android:background="#1A000000"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
