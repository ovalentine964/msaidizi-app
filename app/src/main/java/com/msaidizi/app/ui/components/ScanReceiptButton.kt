package com.msaidizi.app.ui.components

/**
 * Scan Receipt Button — launches camera for receipt scanning.
 *
 * NOTE: This file requires Jetpack Compose dependencies which are not
 * yet in the project's build.gradle.kts. To enable:
 * 1. Add `buildFeatures { compose = true }` to android block
 * 2. Add Compose BOM and material3 dependencies
 * 3. Restore the @Composable implementation below
 *
 * For now, this is a placeholder stub. The receipt scanning feature
 * is accessed via ReceiptScanActivity directly.
 */
object ScanReceiptButtonStub {
    /**
     * Check if Compose-based receipt scanning is available.
     * Returns false until Compose dependencies are added.
     */
    fun isAvailable(): Boolean = false
}
