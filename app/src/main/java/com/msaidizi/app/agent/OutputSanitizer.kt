package com.msaidizi.app.agent

/**
 * Stub: Output sanitizer for agent responses.
 */
class OutputSanitizer {
    fun sanitize(text: String): String = text
    fun sanitizeAmount(amount: String): String = amount
}
