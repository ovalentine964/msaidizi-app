package com.msaidizi.app.onboarding

/**
 * Supported languages for agent naming and interaction.
 *
 * Each language maps to a Locale for TTS and a set of naming prompts.
 */
enum class Language {
    KISWAHILI,
    ENGLISH,
    HAUSA,
    YORUBA,
    AMHARIC,
    PORTUGUESE
}
